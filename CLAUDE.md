# CLAUDE.md: Math Case Study Generation Project

## Read fully before acting.

## Operating rules (apply every session, no reminder needed)

1. After any significant decision, design change, or completed task, update CLAUDE.md
   (decisions log in section 9, case library in section 6, backlog in section 13)
   in the SAME response, before ending your turn. Do not wait to be asked.
2. After any change to files, stage and commit with git. Commit messages are ONE
   line only: a single clear sentence naming what changed, no multi-line body and
   no trailers (no Co-Authored-By, no "Generated with") unless Hitaansh explicitly
   asks otherwise. Never leave the working tree dirty at the end of a task.
3. If a decision contradicts something already logged, record the reversal and the
   reason rather than silently overwriting.

## 0. How to use this file

This is the project memory. It captures everything decided and built so far. The ACTIVE TASK is in section 14. Keep this file updated as decisions are made: add to the decisions log, the case library, and the backlog; prune anything superseded. Never contradict a logged decision without recording why.

## 1. Project in one paragraph

Hitaansh (junior CS student, contract engineer for MathGPT.ai) is building an AI pipeline that generates business-school-style case studies for math courses: one realistic decision a protagonist faces, solved through 2 to 4 interlocking quantitative questions, with a planted misconception trap, a group discussion element, and a fully verified answer key, delivered as compiled LaTeX worksheets. Target audience: struggling community college students. First subject: Calculus 1, then Linear Algebra and Intro Finance, with an architecture meant to scale to any quantitative subject. Prototype pipeline due end of August 2026. Team: Hitaansh plus two other interns.

## 2. People, product, constraints

- **Peter**: Hitaansh's boss/contact at MathGPT.ai. Communicates by iMessage and email. Gives difficulty feedback; his taste is the calibration signal. He generated the taco-truck case (via his own Claude, simplifying our Alder Creek bridge case) and declared it the target difficulty for the community-college tier.
- **MathGPT.ai** (per July 2026 web search): instructor-led learning platform, in 1,200+ courses across ~150 higher-ed institutions; Socratic, cheat-proof AI tutor that guides rather than answers; per-student problem variations; assignment manager; OER/textbook support. Our generator is a content engine feeding it. Open question: which web math renderer their frontend uses (KaTeX/MathJax), for the in-platform display path.
- **Textbook**: OpenStax (Calculus Volume 1 for Calc 1). Chosen over Stewart for licensing, cost, and MathGPT's OER support. CAUTION, logged: OpenStax uses a NonCommercial license and carries a notice about LLM ingestion; confirm commercial and AI-use terms with OpenStax before shipping. Not legal advice; verify.
- **Peter's requirements so far**: Calc 1 only for now; simplify for a less sophisticated audience (we answered with scaffolding, not dilution; see decisions log); add flashcards and quizzes (promised, not yet built; they should be generated FROM each case's objectives, a retrieval-practice layer around the case); every case ships with a textbook/learning-objective alignment block.
- **Peter's backend directive (July 20, 2026; CORRECTED same day, then BUILT)**: a local MySQL FLASHCARD database (MySQL because the production system is already on MySQL). Our first same-morning interpretation (a corpus/content retrieval layer mirroring book_map.json) was WRONG; fuller meeting notes from Hitaansh settled it: the DB stores flashcards tagged by subject/textbook/chapter/LO, one-to-many LO to cards, content as blobs, two card types (concept/example and problem/solution). TEAM VISION CHANGE (decided while Hitaansh was on vacation): flashcards are per-concept pairs, a section's N concepts each get a concept card (concept front, worked example back) and later a problem card (problem front, ANIMATED solution back; GIF vs browser-rendered JSON undecided, team finalizing); the repo's existing 5-card LaTeX sheet flashcard model (phase2_flashcards_prompt_v1) is superseded on this path. Data structure stays separate from UI; any presentation layer plugs in. Built July 20, concept cards only: see the sec 9 decision block and flashcards_db/.
- **Peter's two-phase directive (July 2, 2026, supersedes parts of the sec 7 plan; re-planning pending)**: the pipeline splits into a MINIMUM of two phases. Phase 1: input = one source problem (problem.txt), output = 5 files: primary.md (PRIMARY OER section extract with license/attribution metadata, LOs, topics, content), supporting_01.md and supporting_02.md (SUPPORTING section extracts, same format), lo_mapping.json (problem LO, PRIMARY/SUPPORTING section citations with justifications, confidence rubric, critique_findings, missing_concepts, primary/supporting assessments, recommended_changes), verified_answer.txt (fully worked verified answer to the SOURCE problem). Phase 2: input = those 5 files, output = (1) a case study like our current ones, (2) a set of flashcards on the problem's topic. Any-subject by design: the fence comes from the input problem plus cited sections, not a typed subtopic. Example in /phase1example (simplex pivot problem mapped to Kuttler/Austin linear algebra sections; verified answer re-derived by us July 2 and confirmed correct). Interface matches the recon's human source inputs (problem.txt, primary.md, supporting_*.md, lo_mapping.json, verified_answer.txt): Peter is having phase 1 GENERATE what his IMathAS pipeline required humans to supply. Phase 2 prompts are done and tested (July 2); current focus per Hitaansh (July 5): build the phase 1 prompts (sec 14).

## 3. What a case study means here (settled definition)

- One cohesive real-world decision faced by a named protagonist. The questions interlock: later parts need earlier results, and the recommendation needs all of them. NOT a multi-part word problem with unrelated parts (Peter's original complaint about other models).
- Difficulty benchmark: **the taco truck** (Peter's). Diego weighs a second truck. Arrivals C(t) = 120t - 30t^2 people/hour, service cap 100; peak via C'(t)=0 at t=2 gives 120 > 100. Growth g(t) = 80 - 4t per month; integral over 12 months = 672, so 300+672 = 972 > 600 threshold. Trap: 300 + 80(12) = 1,260 overcounts because a changing rate cannot be treated as constant. Verdict: buy. Two clean calculus moves, one decision, clean verified numbers.
- A harder tier exists (Alder Creek original, Terranova Pueblo, Cedar & Sage conversions) built on hidden-method, single-question, competing-hypothesis design. Parked for now; community-college tier is the product.
- The permanent tension, resolved: true HBS cases have no single right answer; our autonomy/verification requirement demands one. The bridge: **path ambiguity with answer uniqueness**: multiple plausible analytical routes, exactly one survives the numbers, tempting wrong routes flip the decision. Residual open judgment goes to a rubric layer, never the verifier.

## 4. Design principles (the distilled system)

Twelve invariants (stated once in the prompt, subject-agnostic): (1) one decision, interlocking parts; (2) compute-interpret-decide arc, no computation left uninterpreted; (3) misconception trap with decision stakes, both numbers shown; (4) full-strength concepts, never degenerate versions; (5) minimal faithful instance, numbers worked backward from clean answers; (6) grain-size time budget (chunky subjects = 1 concept layered, fine-grained = 2 interlocking; 15-20 group minutes); (7) context must constrain (strip test; inert distractors allowed only as obvious flavor); (8) method concealment (technique names never student-facing); (9) scaffold, don't dilute (optional checklist, fixed rigor); (10) honest verdicts (rotate outcomes; not every case says yes); (11) exactly one genuine discussion element, about judgment not computation, default = the trap as a teammate's claim with a checkable resolution; (12) verify everything, keys show operator-bearing work per boxed answer with skill labels.
Plus: **derive-then-generate playbooks**. Subject rules are instances of the invariants; for a new subject the model derives a playbook (P1 scope fence, P2 grain size, P3 full-strength forms, P4 clean-number conventions, P5 the trap, P6 authentic users, P7 interpretation demands, P8 reference data: constants, formulas, critical values students need but cannot derive, supplied in the data block without naming the technique) before generating, emitted as LaTeX comments for human harvest into a vetted library. The syllabus fence (subtopic) can never be derived; it is a required input. (SUPERSEDED on the phase-2 path, July 2, 2026: see the reversal in sec 9; v5's manual subject/subtopic mode unchanged.)

## 5. Prompt lineage

- v1: Calc 1 only, markdown output, taco-truck anchored.
- v2: LaTeX output contract, one-page student sheet, collaboration requirement, title/mini-title rules (stole layout discipline from Peter's finance worksheet prompt; rejected its single-concept rule and its subtopic-in-title header, which leaks the method; it also had zero verification machinery). Later edit: concept selection made input-driven (sections rule; optimization+accumulation demoted to default; alternative pairings listed; trap adapts to integral-free scope since integration arrives late in Calc 1).
- v3: master prompt, shared core + subject modules for calc1 / linear_algebra / intro_finance (module idea generalized from Peter's Linear Algebra prompt's topic-specific rules; its coffee-shop gold standard verified correct: x=9, y=6, z=15, total 30 < 35, verdict don't buy, which inspired the verdict-variety rule). Finance anchor written by us (5-year loan: 8% simple = $7,000 beats 7.5% compounded = ~$7,178; sticker-rate trap).
- **v4**: `prompts/universal_case_study_prompt_v4.md` (superseded by v5; move to prompts/archive/ on next sync with the full repo). Universal: 12 invariants + mandatory derivation step + vetted playbook library (the three v3 modules reformatted as playbooks) + harvest mechanism (derived playbooks as trailing LaTeX comments). Inputs: subject, subtopic (required, syllabus fence), audience, domain, case_number, key_placement (instructor_pages default | same_page), playbook (optional, skips derivation). Fixed grayscale pdflatex preamble is the compile guarantee. RECENT EDIT already applied: page count demoted to a preference; required content (including skill labels) always beats page fit; the 80-95% fill rule removed. This edit resolved a real internal contradiction (invariant 12's skill labels vs the old same_page 2-to-4-line compactness rule) that we discovered by violating it during a test generation.
- **v5 (CURRENT for the manual subject/subtopic path)**: `prompts/universal_case_study_prompt_v5.md`. Product of the section 14 external review, triaged July 2, 2026 (reviewer verdict: ship with edits; scores: enforceability 4, cohesion 8, scalability 5, LaTeX 7, clarity 6). Version skew confirmed: the reviewer quoted the removed 80-95% fill rule, so they reviewed the pre-relaxation copy and their page-fit findings were largely already fixed. Review depth gauge: they did NOT catch the already-fixed skill-label/same_page contradiction, nor the "about ten"/twelve invariant-count mismatch, confirming a single pass is not exhaustive. Accepted edits (some modified): P8 REFERENCE DATA added to the derivation step and all three playbooks (structurally sanctions the formula-sheet move from the old stats caveat; finance's one-off "formulas may be supplied" allowance generalized into P8); hint content rule in invariant 9 with good/bad examples (hints nudge at scenario level, never name or paraphrase the technique; this was the highest-frequency concealment leak); verdict chosen BEFORE data is built with "proceed" needing justification (replaces the unenforceable stateless "rotate across cases" wording); three ruled lines under the group recommendation (resolves the genuine no-answer-space vs written-recommendation contradiction); % VERIFICATION comment block before \end{document} (visible arithmetic per boxed answer, doubles as a diff target for S3/S4); no-code-fence rule plus an explicit non-ASCII offender sweep; blank-subtopic behavior defined (playbook default used with a flagged assumption comment, else a one-line error comment and no case); trap number must itself be clean and the gap must flip or nearly flip the verdict (invariant 3 and P5); soft sizing guide with countable budgets (scenario 90-130 words, 3-4 questions, data block about 6 rows), content completeness still beats page fit; revision_rules scoped to user-requested follow-up turns only; "about ten" corrected to twelve; footer placeholders made explicit. Rejected, with reasons: deleting revision_rules (manual iteration with Peter is a real usage path; scoped instead); the reviewer's 25-35% bloat trim beyond true duplicates (checklist redundancy is deliberate defense in depth per section 8); dropping enumitem/array from the preamble (compile-guarantee stability beats minimalism); any prompt-side fix for the self-graded strip test (S6 judge owns that check); no change for invariant 4 vs 5 tension (the taco-truck anchor is the calibration mechanism, and no concrete edit was proposed).
- **phase1 v1 (CURRENT for the file-driven path, July 6, 2026)**: `prompts/phase1_generator_prompt_v1.md` + `prompts/phase1_critic_prompt_v1.md`, built per plan/phase1_prompt_design.md and plan/phase1_implementation_plan.md. Generator: corpus preflight, problem LO (singular + per-part array), solve with substitute-back and write verified_answer.txt, 4-dimension rubric search over book_map.json files (confidence_score = median, bands LOW < 0.5 / MEDIUM 0.5-0.75 / HIGH > 0.75, rubric_scores = the PRIMARY's scores), house extracts with stable {#book_tag-sec-kind-n} anchors, mapper-half lo_mapping.json with critic sentinels untouched; honest no-coverage path (no_primary_available + fallback_sections + missing_concepts, never inflated scores); failure = one-line phase1_error.txt and nothing else. Critic: independence-preserving calibration gate (re-solve BEFORE opening any draft; mismatch or silently omitted part-final = exact error line, halt, drafts untouched), 6-item adversarial audit ledger with mandatory non-findings, REPORT-ONLY critique fields, may verify/extend missing_concepts. Both tested July 6, all green (sec 6, sec 14).
- **phase2 v1 (July 2, 2026; case prompt superseded by phase2 v2, flashcards prompt still CURRENT)**: `prompts/phase2_case_study_prompt_v1.md` + `prompts/phase2_flashcards_prompt_v1.md`, the phase-2 fork per plan/phase2_prompt_design.md. Case prompt = v5's invariants, contract, and anchor re-grounded on the 5 phase-1 files: ground-truth hierarchy, calibration gate against verified_answer (exact % ERROR refusal line), fence = sections + printed reference data with missing_concepts mandatory in the data block, fresh numbers, P1-P8 derived from the files (supplied playbook constrains P3-P7 style only), defensive weak-mapping % WARNING banner above \documentclass (opening rule amended accordingly), grounded alignment block + % ATTRIBUTION comments (CC BY credits from file metadata), LaTeX-safety rule (never copy section LaTeX; PRIMARY notation conventions). Flashcards prompt = independent artifact, no concealment, flashcard_count default 5, mix 2 definition / 2 procedure / 1 interpretation (40/40/20 general rule), per-card citations in a % SOURCES block (section anchors or missing_concepts indices), Quiz Yourself page + Answers page with matching numbers, student-facing attribution line, house preamble minimally adapted (cardbox, deck footer). Both passed their first test round July 2 (sec 6, sec 12).
- **phase2 v2 MASTER (CURRENT for the file-driven case path, July 6, 2026)**: `prompts/phase2_case_study_prompt_v2.md`. Three-way merge requested by Hitaansh of the three team members' phase-2 prompts: backbone = phase2 v1 verbatim (all logged decisions intact), grafts from the two other interns' prompts (raw pastes kept as prompts/p1, p2, p3; p1 = our own phase2 v1 with the angle-bracket tokens stripped; p2 and p3 are the interns' prompts, ATTRIBUTION CORRECTED July 6: first logged here as Peter's finance/LA prompts because they closely resemble those sec 5 ancestors, Hitaansh corrected same day). Grafted from p2 (finance-worksheet style): countable difficulty gauges in invariant 6 (strong student > 8 min, struggling 15-20, per-question minute budgets 4-5/5-6/5-7 rebalanced for a 4th question); new <question_quality> section (no one-step-substitution questions, at least two questions with 2+ distinct steps plus the step taxonomy, max 2 subparts (a)/(b) per question, 6-task cap, deepen-not-messy difficulty menu); answer-key line discipline in <instructor_material> (setup/substitution/simplification/boxed answer on separate lines, no inline chains, interpretation on its own line, never fine print) with matching revision_rules and preflight lines; plus (added July 6 after Hitaansh's push-back, see sec 9) the compile-before-delivering step, made environment-conditional in a new <compile_verification> section (when execution + a TeX engine exist: actually compile, fix, recompile to exit 0 first pass, deliver .tex + .pdf, never claim an unrun compile; plain chat: source only). Grafted from p3 (LA, coffee-shop gold standard): verified_answer's method = the house method for the key (new ground_truth_hierarchy rule 2, ties to the July 2 method-exemplar decision); treat-the-problem-as-a-model-never-a-template line in the scope fence (no reskins of the source story); realistic-domain menu + no-fantasy/sci-fi/artificial-stories rule in P6; instructor-voice question shapes (concealment-safe rewrites of its examples) in <question_quality>; final-question-returns-to-the-decision added to invariant 2. Also tightened: all-math-in-math-mode now explicitly covers the caret/superscript concern, title rule now names the generic-title ban and the no-subtopic-in-title leak. Flashcards prompt unchanged (nothing in p2/p3 touches flashcards). UNTESTED as a whole; every graft is additive over the tested v1 backbone. Superseded by v3 July 7.
- **phase2 v3 MASTER (CURRENT for the file-driven case path, July 7, 2026)**: `prompts/phase2_case_study_prompt_v3.md`. Merge of v2 with Rohan's UPDATED prompt (July 7 revision of prompts/p1, which had itself absorbed our scope fence, strip test, full-strength concept, minimal faithful instance, and verdict-first: real team convergence). Three interface changes, all decided by Hitaansh (reversals logged in sec 9): (1) input contract = 3 REQUIRED files (primary.md, question.txt with problem.txt accepted as its phase-1 name, verified_answer.txt) + 2 OPTIONAL (supporting_*, lo_mapping.json), case must be fully generatable from the three alone; every lo_mapping consumer got a defined absent-path: scope fence derives the gap list itself (mandatory, hidden prerequisite = violation), P1/P8 fall back to question-vs-sections inference, alignment block cites file metadata with a "roles assigned by the generator" sentence, weak_mapping_warning renamed mapping_status with an exact % NOTE line above \documentclass when lo_mapping is absent (WARNING/NOTE/nothing mutually exclusive, preflight-checked), % ATTRIBUTION emits an exact no-metadata line rather than inventing or omitting; (2) title format = [Topic]: [Generated Case Title] per Rohan, topic 1-4 words from lo_mapping's LO topic else primary.md's section topic, as invariant 8's SINGLE sanctioned concealment exception (everywhere else concealment unchanged); (3) PDF = pipeline compile stage is the authoritative producer, prompt keeps the environment-conditional compile+inspect, .tex stays the artifact of record. Also grafted from Rohan's update: the task-counting rule (question without subparts = 1 task; with subparts, count subparts instead; cap stays 6, his is 5, team item to reconcile) and his audience don'ts (no childish wording, no excessive narrative, no tedious arithmetic) folded into question_quality. Static checks run: non-ASCII scan clean (header interpunct only, same as v1/v2), calibration % ERROR line byte-identical across v1/v2/v3. UNTESTED; the no-lo_mapping degradation path has never run and needs its own test (3 files only) alongside the 5-file happy path and the calibration negative control. Same-day amendments (July 7, Hitaansh): hard one-page rule with engineered-fit resolution order (reversal, sec 9); markdown title/lineage header stripped, file begins at <role> (prompt-file hygiene, sec 9); second Rohan-convergence round (sec 9): PDF-only delivery with .tex written to disk, exactly 3 questions with a 5-task cap, and ground_truth_hierarchy rewritten as <file_authority> (per-purpose authority, verified_answer still the mathematical authority).

- **flashcard concept card v2 (CURRENT for concept flashcards, July 28-29, 2026)**: `prompts/flashcard_concept_card_prompt_v2.md`, built per plan/flashcard_json_prompt_design.md and plan/flashcard_json_prompt_implementation_plan.md (8 tasks). Replaces the .pptx-generating deck prompt that produced the 75 imported decks. Neither is in a fresh clone: that prompt was never in this repo, and its output, the OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/ folder, is deliberately untracked and still exists only on Hitaansh's machine (backlog 14(f), the archiving action item). CONTENT ONLY: the model emits one JSON object per card and decides no color, font, size, spacing, page geometry, or file format, because the compliance audit in sec 9 shows those are exactly the rules a prompt cannot hold. Four inputs: topic, book_tag, section, optional source_notes. Everything else (concept name, lo_ordinal, lo_text, title, subtitle, central statement, variable key, both descriptions, the problem, every row, both footers) is derived from the named section, read out of the book_map.json whose book_tag field matches, never remembered. Output contract is enforced mechanically by tools/check_flashcard_json.py, 14 gates, exit 0/2, with 5 fixtures and 81 tests; three validated example cards live in flashcard_examples_v2/. UNTESTED AS A PROMPT: it has never been run, in any session (sec 12).

## 6. Generated case library (files in cases/)

Community-college tier (the product): taco truck (Peter's benchmark, in the prompt as anchor), food_bank (net accumulation: inflow minus outflow vs warehouse cap; overflow 3,680 > 3,500; peak day 6 at 360 > 300; trap 4,320 vs honest 2,880), community_garden (constrained optimization 60x120 = 7,200 sq ft; harvest integral 512 >= 400 promise; trap 768 vs 512), alder_creek_calc1 (forensic bridge, Calc 1 only: flood peak 2,000 < 2,500 scour threshold kills the state's theory; damage rate integral calibrated to D(40)=0.40 predicts failure at T~58.4 yrs = 2023-24; the 2005 report's linear extrapolation is the embedded trap; replacement design 5,500 trucks/day via decaying growth-rate integral).
Test generations under v4 (with caveats, see sec 13): test_stats_recycling (derived-playbook path, one-proportion test: z = -1.0 not significant at n=100, z = -2.0 significant at n=400 with the same 16%, verdict "not yet"), test_finance_cookie (same_page path, markup vs margin: 28.6% at $8.40 vs $10 for true 40%, break-evens 500 vs 300 against 400 demand, launch at $10). Both compile first pass.
Test generations under phase2 v1 (July 2, from /phase1example, in cases/tests/): test_phase2_simplex_case (Copper Kettle Loan Decision: pivot on g via -90, ratios 12 vs 20, honest max P = 1,080 vs Marco's infeasible 1,800 from the wrong pivot row (u1 = -16), bank threshold 1,500 between them, verdict hold; every number re-derived by hand July 2, optimality confirmed by an independent vertex check; % WARNING banner fired correctly on the LOW-confidence mapping; concealment held) and test_phase2_simplex_flashcards (5 cards, 2/2/1 mix, all five citations verified against real anchors/missing_concepts). Negative control passed: corrupted verified_answer (75 changed to 72) produced exactly the specified % ERROR refusal line and no case. CAVEATS: generated by harness subagents that load CLAUDE.md, so semi-fresh at best, and same model family as the drafting session; NOT compiled (no TeX toolchain on this machine); single runs, so derived-playbook variance unmeasured.
Test round under phase1 v1 (July 6, artifacts in phase1_runs/ and cases/tests/): water_tank happy path (fresh Calc 1 problem, generator + critic both green, validator exit 0 at both stages, arithmetic hand-verified: peak 750 gal/hr at t=5, total 4480 vs 5000 threshold, technician trap 6000; mapping PRIMARY 5.4 + SUPPORTING 4.3/5.3, HIGH 0.85 by the median rule; the critic caught a real invented sentence in the 5.3 extract and certified the fact into missing_concepts instead of editing, report-only held). Calibration negative control (4480 corrupted to 4520: critic emitted the exact error line naming critic 4480 vs generator 4520, sentinels stayed pending, drafts intact). Coverage negative control (simplex problem vs calc-only corpus: no_primary_available true, LOW 0.1, sections empty, 7 simplex missing_concepts, no fabricated citations; verified answer matched Peter's golden values y1=15, y4=60, P=75 optimal). End-to-end: test_phase1_e2e_water_tank_case (Frost Night Water Decision: peak 400 at t=4 vs 450 limiter, integral 1800 < 2000 requirement, Dev's peak-times-hours trap 2400 flips the verdict, book the hauler, 2600 with 600 margin under 3000 capacity; % WARNING banner correctly ABSENT on the HIGH mapping; tectonic exit 0 first pass, PDF inspected). CAVEATS: harness subagents (semi-fresh), generator/critic test runs on one model, prompts drafted by a stronger model of the same family, single runs.
Harder tier (archive): alder_creek original (Calc 1/2), terranova_pueblo (single-question, Helios licensing option +$3.4M beats building -$3.3M), cedar_and_sage quantitative conversion. Chat-only, no files: Lumen Labs, Arclight.

## 7. Pipeline build plan (full detail: plan/pipeline_build_plan.md)

Eight stages: S0 intake -> S1 playbook (cache keyed subject+subtopic, approved flag; derivation is a cached pipeline stage, the harvest loop) -> S2 author (LLM emits case_spec.json, NOT LaTeX; schema-validated) -> S3 verify (SymPy re-derives every answer with per-answer evidence rows; trap materiality = wrong and right numbers on opposite sides of the decision threshold; scope-fence expression scan; regenerate max 3) -> S4 render (spec to .tex; lint enforces ASCII, skill labels, derivation sufficiency, concealment scan, and numeric identity: boxed values must string-match verified values, our forbidden-change scan) -> S5 compile (pdflatex first pass) -> S6 judge (LLM checklist ledger incl. strip test and difficulty vs taco truck; advisory flags, not a hard veto) -> S7 package. Terminal machine status: READY_FOR_INSTRUCTOR_REVIEW, never "done"; only a human sets APPROVED_FOR_MATHGPT. Ground-truth hierarchy: SymPy > LLM; pdflatex > LLM claims; approved playbook > model inference. Fixtures with negative controls are the CI (bad cases must keep failing). Key metric: first-try pass rate at S3. Stack: Python 3.11+, anthropic SDK (+optional openai), sympy, jsonschema, jinja2 optional, pdflatex/tectonic, CLI first, FastAPI stretch. Team split: A = prompts and LLM stages (author/judge, v4 derivatives), B = verification core + fixtures, C = render/compile/lint/infra/CLI. Week 1 non-negotiable: freeze case_spec.schema.json first. Definition of done: CLI takes subject+subtopic, emits verified packages for all three subjects, negative controls green, measured acceptance rate and cost per case.
Prompt migration: v4 stays as single-shot manual mode; pipeline derives author.md (JSON out), render.md (verified spec to LaTeX, may not alter any number), judge.md (adversarial ledger) from the same invariants+playbooks source of truth.

## 8. Recon takeaways (recon/PIPELINE_RECON.md, recon/REFERENCE_EXTRACTS.md)

Peter's example pipeline (IMathAS template generator, v3.26.9): prompt repository + 27 Python static validators + 62 schemas, human pastes 7 phase messages into a chat, evidence-gated (SHA-256 + JSON Schema + line-level proof), terminal status READY_FOR_HUMAN_REVIEW_STATIC_VERIFIED never PRODUCTION_READY. We adopted: semantic-before-concrete (their semantic ids -> our case_spec before LaTeX), defense in depth (rule in prompt + linter + gate), evidence-gated completion, read-only validators with one quarantined mutator + computed forbidden-change scan, per-run isolation, adversarial audit with non-findings ledger, fixtures with negative controls, ground-truth hierarchy, derivation-sufficiency checks. We rejected: chat-message orchestration (we automate via API; their biggest gap is no automated LLM layer), their schema/tool scale (we start with ~6 artifacts, ~6 checks, grow per caught failure).
REFERENCE_EXTRACTS.md contains verbatim adaptation-ready code: (1) validator skeleton (exit 0/2, checks dict, report self-validates against its own schema; fixture suites are explicit blocks, no discovery magic), (2) fixture suite pattern (the key negative control: a contract claiming PASS that omits evidence_lines must FAIL), (3) forbidden-change scan (refusal guard unless semantic_safety_verified; comparator is COMPUTED, never attested; \_norm_fmt strips only authorized transforms), (4) line-level evidence rows (strict `is not True` boolean checks, required-field set, ordering, duplicate rejection; mirror for SymPy evidence rows), (5) gate evaluator (closed FINAL_ALLOWED_STATUSES vocabulary, completeness matrix, independent re-derivation instead of trusting earlier reports; our package gate should re-run one SymPy spot check), plus schema conventions (warning: their smallest schemas are permissive stubs that check nothing; don't copy those).

## 9. Key decisions log (with reasons)

- Correctness architecture from day one: LLM authors, deterministic math engine (SymPy) verifies; the model's arithmetic is never the source of truth. "100% correct autonomously" is realistic; "zero human review ever" is a later milestone.
- Narrow-and-deep scope beats broad-and-flaky; Calc 1 first (Peter confirmed Calc 1 only).
- Scaffold, don't dilute, for the struggling-student audience (backed by productive-failure research); optional analyst's checklist is the mechanism; we pushed back on Peter's "simplify" accordingly and he accepted.
- Hide the method, not the answer: technique names never student-facing; data must pin exactly one model (uniqueness check before stripping closed forms).
- Verdict variety: generators that always say yes teach students to stop reasoning (inspired by Peter's coffee-shop "don't buy" verdict).
- Discussion element: judgment, never arithmetic; default is the trap as a teammate's claim (fits MathGPT's 3-5 student rooms).
- LaTeX with a fixed house preamble; grayscale print-safe; page count is a preference, content completeness wins (user: "idrc about the 1 page rule"). (Page-count half SUPERSEDED July 7, 2026: see the one-page reversal below; fixed preamble and grayscale unchanged.)
- Alignment block (OpenStax sections + objectives) ships with every case; instructor-facing only.
- Provider portability: prompt is provider-agnostic; real equalizer is the verification pipeline; expect fence-wrapping and unicode leakage on some models; test per provider via the eval harness.
- Playbook derivation is a cached, human-approved pipeline stage (consistency + cost), not a per-generation step.
- Review triage principle (July 2, 2026, v4 review): accept edits that convert unmeasurable prompt rules into countable or mechanical ones (verdict-first, sizing budgets, P8 reference data, hint examples); keep deliberate checklist redundancy (defense in depth); enforcement for checks a model self-grades (strip test, arithmetic) lives in the pipeline, not in more prompt prose.
- REVERSAL (July 2, 2026, confirmed by Hitaansh): "the syllabus fence (subtopic) can never be derived; it is a required input" is superseded on the phase-2 path. The fence is now pinned by the phase-1 files: the input problem (skills tested) + cited sections (what students are assumed to have been taught) + lo_mapping's missing_concepts (what the worksheet must itself supply as reference data). Reason: a human still pins scope, but by choosing the input problem rather than typing a subtopic; phase 1 extracts the fence from that choice. v5's manual subject/subtopic mode is unchanged.
- Phase-2 case uses FRESH numbers (July 2, 2026): the case exercises the source problem's skills on a new, clean-engineered instance (invariant 5 and verdict-first intact; avoids shipping a case whose key already exists in verified_answer.txt). verified_answer.txt serves as a calibration gate (the model must silently re-derive it before authoring; mismatch = emit an error, no case) and as the method exemplar for key granularity, never as the case's answer key.
- Phase2 v2 master merge triage (July 6, 2026, Hitaansh asked for a best-of-three merge of the three team members' phase-2 prompts: his own phase2 v1 plus the two other interns' prompts p2 and p3): accepted only grafts that are countable, mechanical, or concealment-safe (consistent with the July 2 triage principle); full accept list in the sec 5 v2 entry. Rejected on substance, not authorship, with reasons: p2's exactly-one-page rule and 80-95% page-fill rule (already removed in v4, reversal logged; content beats page fit), its [subtopic]: title format (method-concealment leak, rejected since v2 of our lineage), its single-main-concept and exactly-3-questions rules (grain size is subject-driven per invariant 6), its typography/font-size section (fixed house preamble is the compile guarantee), its bottom-anchored answer key (key_placement modes own layout), its caret/superscript rules as written (moot in LaTeX source; kept only as an all-math-in-math-mode clarification); p3's file priority putting primary.md above verified_answer.txt (our ground-truth hierarchy keeps verified_answer on top for math; lo_mapping plays the LO-source role its primary.md plays), its amsart gold-standard preamble and hyperref/multicol packages (house preamble is the compile guarantee), its one-page hard rule and same-page-key requirement (same reasons as above), its raw question-prompt examples ("Explain what this eigenvalue represents") which name techniques (rewritten concealment-safe instead).
- PARTIAL REVERSAL same day (July 6, 2026, Hitaansh): p2's PDF output was first rejected outright ("our contract is LaTeX source"); Hitaansh pushed back that compiling to PDF before delivering is a genuinely good addition. Resolution: accepted in environment-conditional form as v2's <compile_verification> section. Reason for the condition: a plain chat model cannot run a TeX engine, and an unconditional PDF instruction invites claimed-but-unrun compiles; when execution IS available, compile-fix-recompile to exit 0 converts the contract's "compiles first pass" from self-asserted to verified, which matches the evidence-gated principle (sec 8). The .tex source stays the artifact of record either way. (a) corpus extraction method: pdftotext DROPS inline math in the OpenStax PDF; settled method is PyMuPDF page rendering plus visual reading, and every chapter's review spot-verified extracts against rendered pages (one review's fabrication claim was itself retracted this way: render the next page before declaring content invented); (b) validator hardened against null-valued array fields (PowerShell counts @($null) as 1, so "critique_findings": null would false-pass; -is [System.Array] guards added with a new negative fixture); (c) spec AMENDED post-signoff, logged per operating rule 3: top-level rubric_scores = the PRIMARY's scores (best fallback candidate's on the no-primary path); reason: the ambiguity could nondeterministically flip mapping_confidence and with it phase 2's warning banner; (d) critic gate completeness clause: a verified_answer.txt that silently omits a part's final answer is a calibration mismatch (generator value "not stated"), closing the omitted-answer hole a reviewer found; (e) post-final-review hardenings (July 6): source_course_level may honestly leave the corpus vocabulary for off-corpus problems (e.g. linear_programming), and the validator's critic stage now fails any package still containing phase1_error.txt (stale-error guard); (f) two prompt-level extensions beyond the spec, both in its defensive spirit: the critic's unreadable-package error branch and the generator's closed error-category vocabulary.
- Rohan's-AI feedback triage (July 6, 2026; Rohan built his own merged phase-2 prompt, now in prompts/p1, and his AI compared it against our v2; verdict file: /feedback): accepted ONE edit: post-compile visual PDF inspection added to v2's <compile_verification> (from Rohan's internal_workflow; catches layout defects tectonic exit codes cannot). Assessed as spec disagreement, not prompt quality: most of the feedback judges our v2 against ROHAN's instructions to his AI (one page, exactly 3 questions, PDF-only, no answer space), the mirror image of us defending Hitaansh's logged decisions; those calls belong to Peter/the team, not to either AI. Rejected with reasons: 3-file input contract (Peter's directive + /phase1example + our built-and-tested phase 1 define FIVE files; feedback's "aligned with your planned pipeline" is factually wrong for this repo, and its "assumes retrieval pipeline working perfectly" concern is answered by the weak-mapping warning, which exists precisely because the mapping may be weak); primary.md-as-pedagogical-authority hybrid (misreads our file semantics: our primary.md is a textbook extract, not instructor intent; the LO lives in lo_mapping and P1/P2 already consume it); optional trap (trap is in the product's one-paragraph definition, Peter's benchmark contains one, Thompson research backs it; artificial-trap risk is handled by verdict-first + strip test + materiality, and phase 2's real guard is the pipeline); optional method concealment (settled decision, fits MathGPT's Socratic/cheat-proof platform; their "name the method when primary.md's intent says so" is unimplementable on our primary.md); dropping the taco-truck anchor and domain menu for scenario-anchoring risk (anchor is the calibration mechanism per the v5 triage; anchoring risk is real but measured at S6/variance tests, not fixed by deleting the only difficulty gauge); PDF-only-no-source delivery (kills the S3/S4 diff targets, the % VERIFICATION block, and CC attribution, a license obligation, not optional metadata); one-page/3-questions/no-ruled-lines (all previously logged reversals or accepted v5 edits). Noted for the TEAM, the real finding: the three interns have diverged on the Stage 1 interface (Rohan: 3 required files, question.txt, lo_mapping optional; us: 5 files, problem.txt, lo_mapping required per Peter's example). That contract must be settled with Peter before any shared Stage 2 prompt means anything. Also flagged to pass to Rohan: the 5-vs-6 task-cap contradiction his AI found is in HIS prompt (difficulty_requirements 5 vs quality_check 6), not ours (v2 is 6 in both places), and his prompt ships no attribution despite building on OER extracts. (a) corpus = local textbook copies in references/ (per book: book_map.json search index + sections/ content files), no web access at run time; (b) TWO agentic prompts, generator (solve, search, extract, map) + critic (fresh session, adversarial audit), mirroring the mapper/critic split inside Peter's lo_mapping.json; (c) lo_mapping.json clones Peter's example schema exactly, dead fields carry documented empty values (compatibility with his pipeline beats schema cleanliness); (d) verified_answer.txt trust = the critic re-solves the problem from scratch BEFORE reading any draft and halts phase 1 on mismatch (phase 1's twin of the phase 2 calibration gate; also the future fixture negative control); (e) critic is REPORT-ONLY: fills critique fields, never rewrites the mapper's work (read-only-validator principle; weak mappings ship with honest critique, like Peter's example); (f) first corpus Calc 1/OpenStax only; the /phase1example golden diff is deferred until Hitaansh adds the linear algebra books.
- PARTIAL REVERSAL (July 6, 2026, Hitaansh, second pass on the /feedback triage): scenario anchoring from the taco-truck anchor and the P6 domain menu is accepted as a REAL risk, upgrading the same-day triage entry that had rejected it. Evidence is our own library: under anchor-carrying prompts the model chose Copper Kettle (restaurant equipment/loan vs bank threshold, the taco truck's decision shape reskinned) and Frost Night (farm water), and with the human-seeded cases the library runs food-adjacent small business in roughly six of ten. The v5-era "anchor is the calibration mechanism" decision stands, but its recorded basis was "no concrete edit was proposed"; concrete edits now exist. Triage of the three fixes: (1) ACCEPTED, in v2 now: an ANTI-IMITATION GUARD under the anchor (anchor calibrates difficulty, arc, and trap mechanics only; the anchor's food-service domain, buy-a-second-X decision shape, and default-buy verdict are banned unless the source problem points there; P6 menu declared illustrative, never a closed list) plus a matching preflight line; (2) ACCEPTED as pipeline work, folded into backlog 9: an anchor POOL at S0 intake, since rotation must live where state lives (same logic as verdict-first replacing "rotate verdicts across cases"); pool seeds already verified: taco truck, Peter's coffee shop (LA), the v3 finance loan; (3) REJECTED: moving the anchor to instructor-only calibration notes (the anchor's job is to be seen by the generator at authoring time; a prompt has no instructor-only channel, so this equals deleting it and re-loses the only difficulty gauge). Domain distribution joins verdict distribution as a measured quantity in the variance tests (backlog 2 and 11).
- REVERSAL (July 7, 2026, Hitaansh): the phase-2 input contract drops from 5 required files to 3 required (primary.md, question.txt/problem.txt, verified_answer.txt) + 2 optional (supporting_*, lo_mapping.json), adopting Rohan's Stage 1 interface. This supersedes the July 6 Rohan-triage defense of the 5-file contract. Reason: team convergence on a shared Stage 2 interface; our phase 1 still PRODUCES all five files and they are pasted whenever they exist, so nothing is lost on our own pipeline; the prompt must simply also run when only the minimum three arrive. Cost accepted knowingly: without lo_mapping the scope fence and reference data rest on the generator's own gap inference (self-graded), the mapping-confidence warning cannot fire, and the alignment block is generator-attested; the % NOTE line above \documentclass marks those runs so the pipeline and instructors can tell the modes apart.
- REVERSAL (July 7, 2026, Hitaansh, explicit: "I changed my mind"): the topic/subtopic MAY appear at the top of the worksheet, as Rohan's title format [Topic]: [Generated Case Title]. This supersedes the no-subtopic-in-title rule held since v2 of the lineage (and its reaffirmation in the July 6 merge triage). Scope of the reversal: the title prefix ONLY; invariant 8's concealment is otherwise unchanged (question text, hints, mini-titles, data block still never name the technique), so recognizing how to apply the named topic remains part of the task.
- PDF placement decision (July 7, 2026): the pipeline's compile stage (tectonic/pdflatex, the old S5) is the authoritative producer of the final PDF; the generation prompt keeps only the environment-conditional compile+inspect from the July 6 partial reversal, and .tex remains the artifact of record. Reasons: a bare LLM cannot execute a TeX engine, so an unconditional in-prompt PDF mandate is unexecutable in plain chat and invites claimed-but-unrun compiles; deterministic compilation belongs in a deterministic stage; and PDF-only delivery would destroy the S3/S4 diff targets, % VERIFICATION, and attribution. End goal stays PDF, guaranteed by the pipeline rather than trusted from the model.
- REVERSAL (July 7, 2026, Hitaansh: "let's make sure the worksheet is maximum one page long"): a HARD one-page rule returns to the master prompt, superseding the page-count half of the "page count is a preference" decision (and the v4-era removal's rationale, which stands as history: the old rule was cut for contradicting invariant 12; the new rule resolves that differently). Scope: instructor_pages = the student sheet (everything before \newpage) fits one page, instructor material unlimited; same_page = the entire handout including the key fits one page. Fit is engineered via the sizing budgets plus a defined 5-step overflow resolution order (tighten wording, compress layout, shorten scenario to its floor, cut optional visual/distractor, drop a subpart or the 4th question and re-verify); required content (data block, trap, hints, ruled lines, boxed answers, same_page skill labels and key line discipline) may never be deleted for fit: if it cannot fit, shrink the case, not the content. Enforcement honesty: in agentic runs the rule is checked against the real page count during <compile_verification> PDF inspection; in plain chat it is self-asserted via the budgets, so the pipeline compile stage is the true gate. Risk accepted and logged in sec 12: this re-approaches the old same_page skill-label contradiction; the resolution order plus one-page-by-design is the answer this time.
- Prompt-file hygiene (July 7, 2026, Hitaansh): prompt files carry NO markdown title/lineage/usage header at the top; they start directly at <role> (or the first contract tag). Lineage and usage notes live in CLAUDE.md sec 5, not in the artifact a user pastes into a model.
- Second Rohan-convergence round (July 7, 2026, Hitaansh picked from the disagreement list): (1) DELIVERY = PDF ONLY, adopting Rohan's contract and superseding the same-day "deliver .tex + .pdf" wording: an agentic run returns just the compiled PDF, but the final .tex is WRITTEN TO DISK beside it (same basename) for the pipeline's diff/verification stages; delivery changed, existence did not, which is how the earlier objections (S3/S4 diff targets, % VERIFICATION, attribution) stay satisfied. Plain chat still returns source only, since a PDF is impossible there. Consequence handled: the CC credit must now be VISIBLE in the PDF whenever metadata exists (instructor_pages: alignment block, as before; same_page: new one-line small gray credit at the bottom of the handout). (2) EXACTLY 3 QUESTIONS and a 5-TASK CAP, adopting Rohan's numbers over our 3-4/6 (his counting rule was already shared); P2, invariant 6 budgets, sizing guide, one-page resolution order, and preflight all realigned. (3) ground_truth_hierarchy REWRITTEN as <file_authority> per Hitaansh's hybrid: each file authoritative in its purpose (verified_answer = mathematical authority incl. house method; primary.md = concept authority for definitions/notation/vocabulary/what-students-know, explicitly barred from adding skills; question = skill authority; lo_mapping when present = mapping authority with critique_findings on top). Everything else from the disagreement list stays ours: calibration-gate failure line, mandatory trap, strict concealment outside the title prefix, ruled recommendation lines, instructor apparatus, attribution, house preamble, anchor + anti-imitation guard, % VERIFICATION, playbook harvest. Fresh-instance question asked and confirmed: the scope fence's model-never-template rule now carries the explicit integration example (concepts carry over, story never does).

- Flashcard DB decisions (July 20, 2026, brainstormed with Hitaansh; spec plan/flashcards_db_design.md, plan plan/flashcards_db_implementation_plan.md, all built and verified same day): (1) approach B, concept as a first-class table between learning_objective and flashcard (six-table chain subject > textbook > chapter > learning_objective > concept > flashcard, plus a flashcard_full view); reason: "one concept card and one problem card PER CONCEPT" makes the concept an entity two cards must share, and UNIQUE(concept_id, card_type) lets the DB enforce that pairing, where the notes-literal five tables would pair by string matching. (2) chapter table rows are OpenStax SECTIONS ("1.1 Review of Functions") with a chapter_number rollup column; reason: LOs live at section level in both the book and our corpus. (3) card content as LONGBLOB with per-side format columns ('latex' now; 'gif'/'json' later); reason: keeps the team's open animation decision out of the schema, honoring the notes' data-structure-separate-from-UI principle. NOTE (July 24, 2026): the "'latex' now" example is stale as a description of current DB contents; the schema itself is unchanged (see the July 24 block below), the format column just now holds 'json' for every populated card, exactly the extensibility this decision was hedged for. (4) repo-independent delivery: self-contained flashcards_db/ folder (compose + init SQL + README + demo queries) runnable with Docker Desktop alone and shareable via the team Google Drive; the deterministic seed generator stays repo-side as a build tool; reason: the team does not share this repo (Hitaansh's constraint), and a .sql dump is the universal transfer format for the unknown production integration path. (5) license metadata (CC BY-NC-SA, credit line, source URL) lives on the textbook row; reason: attribution obligation follows the content. (6) scope: concept_example cards only populated; problem_solution exists in the ENUM unpopulated.

- Concept flashcard import final review fix wave (July 25, 2026): a whole-branch review of the six-task import (sec 9's July 24 entries) found the import itself correct end to end but flagged one CRITICAL, four IMPORTANT, and seven MINOR findings; all fixed in one wave, no second pass. CRITICAL: a later append had fused `.gitignore`'s standalone `tools/` line into `tools/.superpowers/`, silently un-ignoring `tools/` (including the 48MB `tools/tectonic.exe`, a live re-run of the exact backlog 8 incident); restored `tools/` as its own line, dropped the dead `tools/.superpowers/` rule (self-ignored elsewhere via `.superpowers/sdd/.gitignore`'s `*`), added `spot_check.html`, confirmed the seven intentionally-tracked `tools/` files stayed tracked. IMPORTANT: (1) added a test parsing the two COMMITTED SQL files only (never calling either generator) to catch `lo_id` drift between `02_seed.sql` and `03_concept_cards.sql` if only one is regenerated after a corpus change; (2) added tests asserting the committed SQL files equal their generators' in-memory output, since 23 green tests on `build_sql()` said nothing about what was actually on disk; required extracting a `build_sql()` out of `generate_flashcards_seed.py`'s `main()` (pure extraction, no logic changed, hash-verified below); (3) `import_concept_flashcards.py` no longer prints a hard-coded `concepts=75 cards=75`, it counts and asserts the real numbers; (4) `generate_flashcards_seed.py` now asserts the corpus size before writing, not after, matching the importer's existing order. MINOR: added a corpus test asserting every slide's shape tags are a subset of `{sp, pic, nvGrpSpPr, grpSpPr}`, so a future new shape type fails loudly instead of silently vanishing from the completeness guard; corrected `001_Functions.pptx`'s mapping row from `clear` to `judgment` (the card teaches what a function IS, not notation-evaluation, and 1.1 has no definition objective; `003_Function_Notation` is the genuinely clear fit for that same LO); corrected the design spec's stale judgment-case list (`039_Higher_Derivatives` and `044_Rolles_Theorem` both shipped `clear`, only `042_Extreme_Value_Theorem` is a genuine named-theorem judgment case); fixed `verify_flashcard_import.sql`'s chapter-spread comment to name the actual expected counts (14/8/17/18/8/10, not the raw folder counts) and the three cross-chapter decks responsible; dropped a duplicated "json" from the README's format note; commented that `load_mapping()`'s `concept_name` field is a review aid only, never consumed by `build_sql()`. Verification: 27 tests green (23 original plus 4 new), `03_concept_cards.sql` sha256 unchanged at `f20879be8b7bc788387d430efe7dfa7fc955e3f93ac0cc38a03c85e2543767fd` after regenerating both files, `git check-ignore -v tools/tectonic.exe` exits 0. Full report: `.superpowers/sdd/concept_flashcard_import_implementation_plan/final-fix-report.md`.

- Concept flashcard import decisions (July 24, 2026; design plan/concept_flashcard_import_design.md, plan plan/concept_flashcard_import_implementation_plan.md, both approved and built same day): (1) imported the 75 PowerPoint concept decks in OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/ (every deck exactly 2 slides: concept front, worked-example back) into flashcards_db/: 75 concepts and 75 concept_example cards now exist alongside the 45 sections and 195 LOs. The three hand-written placeholder cards seeded July 20 (which overlapped 001_Functions and 002_Domain_And_Range) are replaced by the real deck versions. (2) Card content format is JSON ONLY: front_content/back_content each hold a JSON document (a title plus an ordered blocks array, blocks being type text or math_svg), front_format/back_format both 'json'. Reason: the DB feeds the pipeline, which consumes JSON, and a raster render is data that a pinned renderer can regenerate deterministically from that same JSON, so storing a rendered image would mean storing what can already be regenerated. REVERSAL, same session (operating rule 3): an earlier turn in this session chose "both formats" (JSON plus a rendered PNG, via a new flashcard_render table); Hitaansh reversed it the same session with the reasoning above, which also removed the new table, a fourth init file, the PowerPoint COM render step, and the package-size risk all three carried. (3) Schema deliberately UNCHANGED: no new tables, no new columns. The six-table core stays exactly as verified July 20, since it is the artifact Peter's production integration may inherit, and the existing blob-plus-format-column design already absorbed this new content format without alteration, which is exactly what it was hedged for on July 20. (4) Mapping policy: best fit plus a flagged review file, not a hard fail on ambiguity. Reason: keeps the DB complete while keeping the judgment visible rather than buried in generated SQL. Each deck's concept name and section/LO were chosen from the card's actual front-slide content, not its filename; every one of the 75 assignments loads, and the 8 that required judgment are recorded with reasoning in tools/flashcard_lo_mapping.csv, the human review gate for this import. Real finding surfaced by the mapping: three decks teach content whose OpenStax objective lives in a different chapter than the deck's folder (004_Average_Rate: folder ch1, objective 3.4; 022_Limits_At_Infinity: folder ch2, objective 4.6; 062_Indefinite_Integrals: folder ch5, objective 4.10), which is why cards per chapter land at 14/8/17/18/8/10 rather than the folder counts 15/9/16/16/9/10. (5) Source decks (8.3MB) are NOT committed; the decision is to archive them off-repo, to the team Google Drive beside flashcards_db/ (ASSUMPTION, not yet confirmed by Hitaansh, defaulted to the reversible option: committing them later is a simple `git add`). The physical copy has NOT yet been made: as of this entry the folder exists only on this machine; see backlog 14(f) for the open action item. Reason: .git is already 44MB and backlog 8 already flags repo bloat as an open exposure; with the PNG defect meaning there is no raster copy to fall back on (see sec 12), the decks are now the only record of the visual design, so archiving them somewhere deliberate is load-bearing, not optional.

- Flashcard JSON prompt decisions (July 28-29, 2026; design plan/flashcard_json_prompt_design.md approved by Hitaansh July 28, plan plan/flashcard_json_prompt_implementation_plan.md, 8 tasks, all built and reviewed): (1) THE MEASUREMENT that motivated everything here, taken across all 75 decks in OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/ with the repo's own extractor, not estimated: "exactly six meaningful solution rows" was obeyed 0 times out of 75 (6 decks have 3 rows, 50 have 4, 19 have 5); the Unicode superscript ban was violated in 52 of 75 decks measured over superscript digits, superscript n, subscript digits and U+2212, and in 55 of 75 under the wider character set the shipped G11 actually enforces (which adds the rest of the superscript/subscript block plus the times sign, division sign, and caret; the 3 extra decks are 017_One-Sided_Limits, 022_Limits_At_Infinity, 050_LHpitals_Rule). State the character set whenever quoting this number: both counts reproduce exactly and were re-measured independently July 29, but a bare "52 of 75" does not reproduce, since neighbouring character sets give 41, 44, 50, and 55, and 55 is the decision-relevant one because it is what the validator enforces. All 351 .png parts inside the decks are SVG bytes, so 0 are PNG; and 0 of 75 use the native Office Math the prompt named as its first preference. Rules the model could copy or count held at or near 100 percent over the same 75: the exact footer string 75/75, the back title 75/75, the 2-to-4-word subtitle 75/75, the variable-key presence rule 75/75, the 24-character title cap 74/75. Same pattern this log already records as a triage principle: convert unmeasurable rules into countable ones, and enforce anything self-graded outside the prompt. (2) THE SPLIT, decided from that data: the prompt carries content only, and every visual decision (palette, type, size, spacing, geometry, output format) moves to the renderer, tools/render_flashcards.py. Bytes the model cannot inspect and inches it cannot measure are not its job. (3) REVERSAL of the old prompt's row rule: the back row count goes from EXACTLY 6 to a RANGE of 4 to 6. Evidence is the 0/75 above; the model preferred fewer real rows over padding, which is the correct pedagogical instinct and is what the same prompt asked for in its own "no filler rows" line. The prompt was wrong, not the output. 4 is the observed modal count and becomes the floor, 6 stays the ceiling. (4) AMENDMENT to the July 20 flashcard DB vocabulary: the card's LO reference is `lo_ordinal`, a 1-based index into the section's learning_objectives array, NOT `lo_id`. Reason: the corpus assigns objectives no identifiers at all, and the DB's lo ids are autoincrement values created at seed time, so neither is a thing a prompt can name. Gate G13 then compares `lo_text` byte for byte against the corpus objective sitting at that ordinal, which is what makes a paraphrased, drifted, or invented objective impossible rather than merely discouraged. The honest-gap path (`lo_ordinal: null`) requires a `review_note` and an empty `lo_text`. (5) THE VALIDATOR IS THE CONTRACT, not the schema file: `flashcards_db/card_schema_v2.json` is GENERATED from the validator's constants by tools/generate_card_schema.py and pinned by a drift test, and it is a strict SUBSET whose own description names the ten gates it cannot express (8 not expressed at all, 2 partially). Reason: two hand-maintained copies of one contract drift apart, and the executable copy is the one that actually runs; a generated schema stays a convenience for external consumers without becoming a second source of truth. (6) TWO JSON FORMATS WILL COEXIST, knowingly, but DO NOT YET, and nothing in this build made the DB able to serve a v2 card. As of July 29 flashcards_db holds only the 75 legacy v1 cards (title plus an ordered blocks array carrying pre-rendered math SVGs); no v2 card is in it, there is no importer for card JSON, and no front_format/back_format value has been chosen for v2. The renderer's `fetch()` also does not set `format_version` at all: it json-loads the two blobs and stops, and `format_version` lives on the card WRAPPER rather than on either side, which `load_files` compensates for explicitly and `fetch` has no equivalent for, so a v2 row pushed into the DB today falls through to the v1 renderer and raises KeyError: 'blocks'. `--file` is therefore currently the ONLY way to render a v2 card. What IS decided: the renderer dispatches per side on `format_version` and draws both formats in the same deck styling, so the rendering half of a mixed library is done; and the legacy cards' LaTeX is unrecoverable from their SVGs (sec 12 caveat b, no annotation, no title, no aria-label), so converting them is a regeneration through the new prompt, 75 runs plus review (backlog 14(j)), not a data migration. What is OPEN, all of it backlog 14(k), and all of it required before a mixed preview is real: the importer, the format value, the matching `fetch` change, and whether `format_version` rides inside the blob or earns its own column.

## 10. Research foundations (from the July session's literature run)

Forensic/failure case studies are an established engineering pedagogy (Delatte's Beyond Failure, ASCE workshops). Direct product ancestor: Vanderbilt's Jasper Woodbury anchored instruction (narrative problems, data embedded in story, students generate subgoals; validated across 17 classes/7 states). Model-Eliciting Activities supply the design-principles pattern (client-driven, self-assessment principle -> our holdout-data trick). Productive failure (Kapur): struggle before instruction improves concepts and transfer -> hint policy must not reveal the model early (aligns with MathGPT's Socratic tutor). Pseudocontext (Meyer/Boaler) -> the strip test. Thompson: rate-vs-accumulation is the deepest documented Calc 1 weakness -> rate-to-total is the highest-value archetype. Archetype library: forensic post-mortem, rate ledger with threshold, model identification (with holdout validation), competing flows, design to margin, honest error.

## 11. Working conventions and user preferences

- NEVER use em dashes in anything written for Hitaansh or for messages he sends (texts to Peter, docs). Use commas, colons, or restructure.
- Messages to Peter: keep Hitaansh's casual, direct texting voice; when he supplies a draft, edit minimally (strip hedges, add substance) unless asked otherwise.
- Every number in every case must be re-derivable; when producing a case, verify arithmetic before presenting; state honest caveats (this project's culture is flagging our own violations).
- Files: snake_case names; cases ship as .md or .tex+.pdf; prompts as .md.
- Prompt files start at <role> with no markdown title/lineage/usage header; lineage lives in CLAUDE.md sec 5 (Hitaansh, July 7).

## 12. Known caveats (post-review, July 2, 2026)

- The phase2 v1 prompts passed their first test round July 2 (see sec 6: case, flashcards, and the calibration-gate negative control all green), and both outputs COMPILE, exit 0 first pass, under tools/tectonic.exe (installed July 2; one cosmetic overfull-hbox warning in the case's weak-mapping note). Residual caveats: tectonic is XeTeX-based, so strict pdflatex-on-TeX-Live parity is unverified (the plan blesses tectonic as the alternative engine; spot-check pdflatex once somewhere); the test agents load CLAUDE.md so the runs are semi-fresh, not clean-room; single runs on one model family, so variance and portability are unmeasured.
- v5 has had ZERO test generations. The v4 tests were contaminated (produced with full project context) and v5 changes behavior-relevant text (verdict-first, P8, hints, ruled lines), so fresh-session runs on a novel subject are the real test; they also check the review's untested stress-test predictions (stats critical values, now covered by P8; physics constant conventions, now forced by P4's "pick one and say why").
- RESOLVED in v5: the stats concealment collision (old formula-sheet ad hoc move) is now structurally sanctioned via P8 reference data.
- same_page output remains the least-tested path.
- Residual limits accepted at triage, enforcement stays in the pipeline: the preflight is still partly self-asserted (the % VERIFICATION block makes arithmetic visible, but S3/SymPy is the real gate); the strip test is self-graded (S6 judge owns it); verdict distribution across a course pack is unmeasured (v5 biases against default-yes per call, but distribution control belongs at S0 intake).
- Phase 1 v1 passed its full first test round July 6 (happy path, calibration and coverage negative controls, end-to-end into phase 2 with tectonic compile; results in sec 6). Residual caveats: all runs are harness subagents that load CLAUDE.md (semi-fresh, not clean-room); generator/critic test runs on one model and single runs, so variance and cross-provider portability are unmeasured; the corpus is one book (OpenStax Calc Vol 1, chapters 1-6, 45 sections); the /phase1example golden diff is still deferred until the linear algebra books are added; book_map.json's license_url is honestly empty (the PDF prints no bare CC URL anywhere); Hitaansh to decide whether to add the canonical URL from outside the PDF.
- phase2 v3 (the July 7 master) has had ZERO test generations; v2 also never ran, so the tested artifact is still the v1 backbone two merges back. Highest-risk untested surface: the no-lo_mapping degradation path (generator-derived gap list, % NOTE line, generator-attested alignment block), which exists in no tested ancestor. Natural first round: Copper Kettle regeneration from /phase1example with all 5 files, the SAME source with only the 3 required files (diff the two data blocks and banners), and the calibration negative control rerun.
- The topic-in-title reversal (July 7) weakens method concealment by exactly one channel; whether the named topic materially shortcuts tool-recognition for students is unmeasured and belongs in the S6 judge checklist and any real-student trial.
- The hard one-page rule (July 7) re-approaches the v4-era same_page contradiction (skill labels + key line discipline vs one page). The resolution order and one-page-by-design sizing are the intended answer, but a plain chat model cannot measure a page: off-pipeline runs self-assert fit, and only <compile_verification> or the pipeline compile stage actually checks it. Watch the first same_page test for content-vs-fit pressure.
- The 15-to-20-minute timing claim has never been validated with real students.
- Flashcard/quiz layer promised to Peter, not built. (PARTIALLY SUPERSEDED July 20: the flashcard DATA layer now exists as flashcards_db/; card generation at scale and quizzes remain unbuilt, and the team's new per-concept card model replaces the from-case-objectives plan.) (FURTHER SUPERSEDED July 24: concept_example cards now exist at scale, 75 of them, imported from OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/, see sec 9's July 24 decisions and the verification entry above. Still unbuilt: problem_solution cards, pending the GIF-vs-JSON animation call, and quizzes.)
- flashcards_db verified July 20, 2026: mysql:8.4 under Docker 29.1.3, init auto-apply clean, counts 45 chapters / 195 LOs / 3 concepts / 3 cards, LaTeX blob round-trip byte-intact, negative controls rejected as designed (ERROR 1062 duplicate card type, ERROR 1452 phantom concept), coverage-gap query 193, generator determinism hash-checked. Residual caveats: column naming is a guess at Peter's production MySQL conventions (expect a rename pass at integration); the per-section CONCEPT INVENTORY does not exist anywhere yet (the 3 sample concepts are hand-picked; enumerating concepts and authoring cards at scale is future generation work); animation format (GIF vs browser JSON) still open, schema hedged via format columns; compose passwords are local-dev defaults. SUPERSEDED IN PART July 24, 2026 (see the entry immediately below): counts, content format, and the coverage gap all changed once the real 75-card corpus replaced the 3 hand-picked samples; this line stays as an accurate record of what was true at seed time, not of the DB's current state.
- Concept flashcard import verified July 24, 2026: mysql:8.4 under Docker, rebuilt from an empty volume so all three init files (01_schema.sql, 02_seed.sql, 03_concept_cards.sql) applied fresh. Counts 45 chapters, 195 LOs, 75 concepts, 75 cards, front_format/back_format both 'json' throughout (no LaTeX-format cards remain; the 3 July 20 placeholders were replaced, see sec 9). Zero invalid JSON blobs, zero cards with an empty title, zero orphan concepts. Blob round trip (tools/check_blob_round_trip.py): all 75 cards' stored front and back hashes match SHA-256 computed fresh from the source decks, 0 mismatches. Both negative controls still reject: ERROR 1062 on a duplicate card type, ERROR 1452 on a phantom concept. Coverage gap: 122 of the 195 LOs still have no card (down from 193 at the 3-card seed; still large by design, since 75 cards cannot cover 195 objectives). Generator determinism proved across two processes with different PYTHONHASHSEED values; flashcards_db/init/03_concept_cards.sql sha256 f20879be8b7bc788387d430efe7dfa7fc955e3f93ac0cc38a03c85e2543767fd. Caveats specific to this import, none yet closed: (a) source-deck defect: every file named *.png inside the 70 image-carrying decks is not actually a PNG, it is SVG bytes with a misleading .png filename, byte-identical to its .svg sibling; PowerPoint renders it correctly via the svgBlip extension, but any consumer that falls back to the raster path (older viewers, Google Slides import, some converters) gets a broken image; the importer routes around this by reading the svgBlip relationship directly rather than the .png part, and whoever generated the decks should be told (backlog 14(g)). (b) formulas remain images inside the JSON, not text or LaTeX: the MathJax SVGs carry no aria-label, no title element, and no annotation element, so the original LaTeX is not present anywhere in the source files; prose (titles, framing lines) is SQL-searchable, math is not, and this is a property of the source files, not a defect introduced by this import. (c) the JSON block format (title plus an ordered blocks array of type text or math_svg) is our invention, unvalidated against Peter's production expectations, the same standing caveat already logged above for the column naming. (d) the 75 concept-to-LO mapping assignments are LLM-derived; 8 of 75 are flagged as judgment calls in tools/flashcard_lo_mapping.csv with a reasoning note each; that file is a human review gate, not a proof of correctness. (e) the visual spot-check (cards rendered from their stored JSON and compared against the original decks, spanning at least one text-only deck and one math-heavy deck) is OUTSTANDING: comparing rendered output against the original decks is real remaining work for Hitaansh, not yet done, since an agent comparing its own extractor's output against itself would be circular and proves nothing. UPDATE July 28: the check's artifact is now the output of tools/render_flashcards.py (flashcards.html, default deck theme), and the old spot_check.html was DELETED because it was misleading: it painted the cream-filled front formulas on a white page, rendering them invisible. Root cause, discovered when a preview showed back-side math "missing": the decks are two-tone by design (front slides navy bg with cream #FAF8F4 math, back slides cream bg with navy #011E4F math) and the stored SVGs bake those fills in, so any single-background renderer hides one side's formulas; render_flashcards.py now normalises all math fills/strokes to currentColor at render time, which is render-side only, the stored blobs are untouched and their hashes unchanged. A first visual pass on card 28 (Power Rule) against the deck confirmed all content renders; the systematic deck-vs-render comparison is still Hitaansh's.

- Flashcard v2 contract, what the July 28-29 build does and does NOT prove. PROVEN: 81 tests green and 5 fixtures each failing on their own gate; the three example cards in flashcard_examples_v2/ pass all 14 gates, exit 0; the generated schema has zero drift from the validator constants; the renderer draws all three cards with every field visible and every formula typeset (verified from a headless-browser screenshot, not asserted). What that buys is one thing only: INVALID card JSON gets caught. NOT PROVEN, and stated plainly because it is the whole point of the exercise: the prompt has NEVER BEEN RUN, not once, in any session. Nothing here shows it produces valid JSON, only that invalid JSON would be rejected. The three examples were written by the same session that wrote the prompt and the validator, so they measure the contract's internal consistency, not a model's compliance with it. The outstanding step is the fresh-session procedure written into plan/flashcard_json_prompt_implementation_plan.md and backlog 14(i): cold session, no project context, paste the prompt with topic Product Rule / openstax_calc1 / section 3.3 (deliberately not one of the three examples), save the reply VERBATIM including whitespace since an edited output measures the editor, run the validator, and log which gates fired. A first run that trips two or three gates is a useful measurement, not a failure: it names the rules the prompt states unclearly.
- Flashcard v2 LIMITATION, real and unclosed: `latex_identifiers` cannot handle ANY LaTeX environment. The environment name tokenizes letter by letter, so `\begin{cases}` yields the junk identifiers a, c, e, s and `\begin{array}{c}` additionally leaks its column spec, all of which G09 then demands the variable key define, against a 5-entry cap. The prompt therefore bans environments outright and rows carry structure instead. This is correct given the tokenizer, but it is a content restriction, not a style choice: a piecewise central formula cannot be authored today. Fixing it means teaching the tokenizer about environments, not relaxing G09.
- Flashcard v2 renderer caveats: a v2 preview needs network access, since MathJax loads from a CDN, and raw `\(...\)` on screen means it did not load (v1 cards remain fully offline, their math is stored SVG). `--math-scale` applies to v1 cards only; v2 math is sized by MathJax against the surrounding text. The v2 layout was inspected at one width (1400px, deck theme); narrow-viewport and print behaviour are unchecked, which is why v2 cards keep 4:3 as a floor and are allowed to grow rather than clip. `--file` and the database are mutually exclusive inputs, so a single page cannot yet mix stored v1 cards with on-disk v2 cards; the renderer supports both formats, the CLI just sources one at a time.
- Process observation from the 8-task build, worth more than any single finding: tasks 1 through 7 each had their first quality review request changes, several on CRITICAL findings (crash-on-wrong-type in G01 and G13, a G09 coverage hole, an embedded prompt example that failed the very gate it illustrated). Task 8's own first review then also requested changes (a false "a mixed DB previews correctly" claim in this file, an underqualified compliance number, and a raw KeyError traceback on malformed --file input), which is what makes it eight of eight. Recorded with its own process lesson: the first draft of this line asserted eight of eight while task 8 was still unreviewed, one data point ahead of the evidence, inside the section whose whole job is separating proven from unproven. In most rounds the defect was in code or text the PLAN specified verbatim, so the plan was confidently wrong at a steady rate and the review loop, not the plan, is what made the validator correct. Read across the repo: plan-specified code that has not been adversarially reviewed should be treated as unverified, whatever its provenance.

## 13. Backlog

1. ACTIVE TASK (section 14): phase 1 COMPLETE July 6; next moves listed there (show Peter, LA corpus + golden diff, pipeline re-map).
1b. PAUSED: freeze case_spec.schema.json / pipeline week-1 tasks; resume after the sec 7 plan is re-mapped onto Peter's two-phase split (phase 1 roughly covers S0/S1 plus retrieval and LO-mapping machinery we never planned; phase 2 roughly covers S2-S7).
2. Fresh-session v5 tests on 1-2 novel subjects (variance measurement; also exercises P8 on a table-dependent subject like stats or physics).
3. Flashcards + quizzes generated from case objectives.
4. Ask MathGPT which web math renderer they use; confirm OpenStax license terms.
5. Real-student timing validation.
6. Build the fixture suite starting from the extracts' patterns.
7. Finish the repo sync (mostly done July 2: cases/, plan/, recon/ now present and committed): still missing prompts/archive/ (v1-v3) and the 4 cases/archive/ harder-tier files; move v4 into prompts/archive/ once it exists.
8. DECIDE (Hitaansh): calculus-textbook.pdf, the full 52MB OpenStax PDF, is committed and pushed to the GitHub remote. Section 2 already logs the OpenStax NonCommercial license caution; a public repo redistributing the full book is a real exposure, and the 52MB blob permanently bloats clones. Removing it properly requires a history rewrite (git filter-repo) plus force push, so it is a deliberate call, and it gets harder the more commits pile on top. Check whether the repo is private as a stopgap.
9. Pipeline: add verdict-category AND anchor-pool controls at S0 intake so verdict distribution and scenario-domain distribution across a course pack are measurable and steerable (v2's anti-imitation guard and verdict-first handle per-call bias only; anchor pool seeds, all verified: taco truck, Peter's coffee shop, v3 finance loan). Variance tests (items 2, 11) should report domain distribution alongside verdicts.
10. SHOW PETER the phase 2 demo: Copper Kettle case + flashcards PDFs (ready; framing notes: the source-top warning banner is a feature, and the calibration gate is the "did the model understand my problem" answer).
11. Phase 2 hardening: truly fresh-session and cross-provider re-tests, now ON THE v3 MASTER (first round: Copper Kettle from /phase1example with all 5 files, the same source with only the 3 required files, and the calibration negative control); one-time pdflatex parity spot-check on a TeX Live machine (tectonic is XeTeX-based).
12. Harvest the phase 2 corrupted-verified_answer negative control into the future fixture suite (feeds item 6); phase 1's calibration and coverage controls (phase1_runs/water_tank_corrupted, phase1_runs/simplex_coverage) are now built and join it.
13a. Workflow skills (July 6): five project skills captured in .claude/skills/ (verifying-case-math, shipping-prompt-contracts, triaging-reviews-with-evidence, extracting-faithful-corpora, maintaining-project-memory), auto-discovered by future Claude Code sessions in this repo. DRAFTS per Hitaansh's retiring-engineer handoff request: written from this project's real history but NOT baseline-tested per the writing-skills TDD discipline (no pressure-scenario runs); test before treating as bulletproof.
14. Flashcard DB DONE July 20 (built + verified; sec 9 decisions, sec 12 results). Follow-ups: (a) copy flashcards_db/ to the team Google Drive and demo it (coverage-gap query is the "what's left to author" pitch); (b) when the team finalizes GIF vs browser-JSON, populate back_format accordingly, no schema change needed; (c) DONE July 24 for concept_example cards: 75 concept decks imported at scale from OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/ (sec 9 July 24 decisions, sec 12 verification); problem_solution cards remain unbuilt, still pending the animation call in (b); (d) rerun tools/generate_flashcards_seed.py when the LA corpus lands (multi-book: add a second textbook row, generator needs a small multi-book extension); (e) reconcile backlog 3's from-case-objectives flashcard idea with the team's new per-concept model, with Peter; (f) NEW July 24: archive the OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/ source-deck folder to the team Google Drive beside flashcards_db/, per the sec 9 archiving decision; it is deliberately untracked in git and currently lives nowhere else; (g) NEW July 24: tell whoever generated the decks that every *.png inside them is mislabeled SVG bytes (sec 12 caveat), since any raster-fallback consumer of the original files will see broken images; (h) NEW July 24, OUTSTANDING: the visual spot-check (tools/render_flashcards.py output against the original decks, sec 12 caveat (e); spot_check.html deleted July 28, it hid front math) has not been done; it is the one verification step no agent could close itself; (i) NEW July 29, OUTSTANDING and the highest-value next move on this branch: run the v2 card prompt COLD per the fresh-session procedure (sec 12, plan/flashcard_json_prompt_implementation_plan.md), then log which gates fired; until this runs the prompt is unmeasured; (j) NEW July 29: regenerate the 75 legacy v1 cards under v2 so the DB holds one format. Their LaTeX cannot be recovered from the stored SVGs, so this is 75 prompt runs plus review, not a data migration, and it is the price of the two-format decision in sec 9; (k) NEW July 29: wire v2 cards INTO flashcards_db, the four open items sec 9 decision (6) lists. Nothing in the July 28-29 build touches the DB: there is no importer for card JSON, no chosen front_format/back_format value for v2, the renderer's fetch() does not set format_version on either side the way load_files does (so a v2 row would fall through to the v1 renderer and raise KeyError: 'blocks'), and it is still open whether format_version stays inside the blob or earns a column (a schema change needs the sec 9 July 24 reasoning re-applied). Until all four land, --file is the only way to render a v2 card.
13. Housekeeping, none blocking (final review triaged all to backlog): .claude/settings.local.json is harness-generated and already ignored via the machine's global gitignore (add to repo .gitignore only if other contributors matter); compound "and"-joined topic keywords appear in about 11 sections across chapters 1-4 (harmless: read by an LLM, not a tokenizer); validator nuance: a null sections array is caught indirectly via the section-entry-keys check, not by a named check (harden if the validator grows); 3.3's 15-example length trims only if search quality measurably suffers; both prompts carry a harmless lineage pointer to plan/phase1_prompt_design.md; NEW July 29: the renderer's default output name flashcards.html is gitignored but flashcards_v2.html is not, so v2 previews should use the default name (or .gitignore gains the second name) to keep the tree clean.

## 14. ACTIVE TASK: phase 1 COMPLETE (built and tested July 6, 2026)

Phase 2 completed July 2; phase 1 designed July 5 (spec plan/phase1_prompt_design.md, signed off; implementation plan plan/phase1_implementation_plan.md, 16 tasks) and built + tested July 5-6 via subagent-driven execution with per-task adversarial review.

Delivered: references/openstax_calculus_v1/ corpus (book_map.json search index + 45 section files, chapters 1-6, every chapter fidelity-spot-checked against rendered PDF pages during review); tools/check_phase1_package.ps1 validator (exit 0/2, self-tested against /phase1example positive + two negative fixtures, hardened against null-valued array fields); prompts/phase1_generator_prompt_v1.md and prompts/phase1_critic_prompt_v1.md (contents: sec 5 lineage); full test round all green (happy path, calibration corruption control, simplex coverage control, end-to-end into phase 2 with tectonic compile; results: sec 6; caveats: sec 12). The e2e Frost Night case PDF is a second Peter-ready demo alongside Copper Kettle.

Next: (a) SHOW PETER the two-phase demo: phase 2 Copper Kettle (backlog 10) plus the new phase 1 water_tank package -> Frost Night case, the full problem-to-worksheet path on our own pipeline; (b) add the linear algebra books to references/ and run the deferred /phase1example golden diff; (c) re-map the sec 7 pipeline plan onto the two-phase split (backlog 1b; phase 1's generator/critic/validator now concretely covers the old S0/S1 + retrieval); (d) truly fresh-session and cross-provider re-tests for both phases (backlog 11); (e) fixture-suite harvest (backlog 12).

## 15. Repo layout and file map

Git: initialized July 2, 2026, branch main, remote origin = github.com/hitaanshjain/casestudypipeline (pushed). Commit after every change per the operating rules.

```
/CLAUDE.md                                  (this file)
/.gitignore                                 (LaTeX build junk, _reference/, runs/)
/prompt_review_request.md                   the review prompt (repo root)
/prompts/phase1_generator_prompt_v1.md      CURRENT, phase 1 generator (problem -> 5-file package)
/prompts/phase1_critic_prompt_v1.md         CURRENT, phase 1 critic (calibration gate + audit)
/prompts/phase2_case_study_prompt_v3.md     CURRENT, phase 2 master (3-required-file contract, topic-in-title, July 7)
/prompts/phase2_case_study_prompt_v2.md     superseded by v3; archive candidate
/prompts/phase2_case_study_prompt_v1.md     superseded by v2; move to prompts/archive/ when archives are added
/prompts/phase2_flashcards_prompt_v1.md     CURRENT, file-driven path (phase 2 flashcards)
/prompts/p1                                 Rohan's Stage 2 prompt (July 7 update; earlier held Hitaansh's flattened v1); p2/p3 (the July 6 merge sources) deleted July 7, recoverable from git history
/prompts/universal_case_study_prompt_v5.md  CURRENT, manual subject/subtopic path
/prompts/flashcard_concept_card_prompt_v2.md  CURRENT, concept flashcards (content-only, emits one card JSON; July 28-29, never run, sec 12)
/prompts/universal_case_study_prompt_v4.md  DELETED July 7 (superseded by v5; recoverable from git history)
/plan/phase2_prompt_design.md               phase 2 design spec (signed off July 2)
/plan/flashcards_db_design.md               flashcard DB design spec (approved July 20)
/plan/flashcards_db_implementation_plan.md  flashcard DB build plan, 5 tasks (July 20)
/plan/concept_flashcard_import_design.md    concept flashcard import design spec (approved July 24)
/plan/concept_flashcard_import_implementation_plan.md  concept flashcard import build plan (July 24)
/plan/flashcard_json_prompt_design.md       card JSON contract design spec (approved July 28; holds the 75-deck compliance audit)
/plan/flashcard_json_prompt_implementation_plan.md  card JSON build plan, 8 tasks (July 28-29; carries the fresh-session run procedure)
/flashcard_examples_v2/                     3 validated example cards (power_rule, intermediate_value, definite_integral) + README; one per contract branch
/flashcards_db/                             SELF-CONTAINED MySQL package (July 20; extended July 24): docker-compose.yml, README.md, demo_queries.sql, init/01_schema.sql, init/02_seed.sql (generated, now hierarchy only, the 3 placeholder cards removed July 24), init/03_concept_cards.sql (generated July 24, 3.8MB, 75 concepts + 75 concept_example cards as JSON); shareable off-repo, needs only Docker Desktop
/flashcards_db/card_schema_v2.json          GENERATED from the validator constants, drift-tested; a strict subset, the validator is the contract (July 28)
/tools/check_flashcard_json.py              card JSON validator, 14 gates, exit 0/2, tracked via git add -f (July 28); THE contract
/tools/test_check_flashcard_json.py         81 tests for the validator, tracked via git add -f (July 28)
/tools/generate_card_schema.py              emits card_schema_v2.json from the validator constants, tracked via git add -f (July 28)
/tools/fixtures/flashcard_json/             5 fixtures: 1 valid card + 4 that each fail one gate (G05, G09, G11, G13)
/tools/generate_flashcards_seed.py          seed generator, tracked via git add -f (deterministic; reads book_map.json)
/tools/import_concept_flashcards.py         concept flashcard importer (extraction + SQL emission), tracked via git add -f (July 24)
/tools/test_import_concept_flashcards.py    27 tests for the importer, tracked via git add -f (July 24; 4 drift/corpus tests added in the July 25 review fix wave, sec 9)
/tools/flashcard_lo_mapping.csv             the 75 deck-to-LO mappings with confidence flags, the human review gate (July 24)
/tools/verify_flashcard_import.sql          verification ledger for the concept flashcard import (July 24)
/tools/check_blob_round_trip.py             blob integrity check, source decks vs stored JSON hashes (July 24)
/tools/render_flashcards.py                 renders stored cards from MySQL to a standalone HTML page (July 28); --section / --theme / --math-scale / --out; default deck theme = navy front, cream back (the decks' own two-tone design); normalises baked-in SVG palette fills to currentColor at render time; output flashcards.html is gitignored. EXTENDED July 29: renders BOTH card formats, dispatching per side on format_version. v1 = stored math SVGs (offline, --math-scale applies); v2 = LaTeX typeset by MathJax from a CDN (needs network). New --file CARD.json reads cards from disk instead of the database, so prompt output previews before any import; it expands its own wildcards, since PowerShell does not, and cannot be combined with card ids or --section. Aligned rows render as one left-aligned block behind a rule, so a derivation reads as one object rather than as separate centred claims
/OpenStax_Calculus_Volume_1_Concept_Only_Flashcards/  the 75 source .pptx decks (8.3MB); deliberately NOT tracked in git (sec 9 archiving decision); to be archived to the team Google Drive
/plan/phase1_prompt_design.md               phase 1 design spec (signed off July 5)
/plan/phase1_implementation_plan.md         phase 1 build plan, 16 tasks (July 5)
/references/calculus-textbook.pdf           the 52MB OpenStax PDF, moved from repo root July 5 (backlog 8 still applies)
/references/openstax_calculus_v1/           corpus: book_map.json + sections/ (45 files, ch 1-6, built July 5-6)
/phase1_runs/water_tank/                    happy-path test package (generator + critic green)
/phase1_runs/water_tank_corrupted/          calibration negative control (phase1_error.txt fired)
/phase1_runs/simplex_coverage/              coverage negative control (honest no-coverage package)
/tools/check_phase1_package.ps1             package validator, tracked via git add -f (tools/ otherwise gitignored)
/cases/tests/test_phase1_e2e_water_tank_case.{tex,pdf}  phase 1 e2e demo (Frost Night, via phase 2)
/.claude/skills/                            five workflow skills (July 6 handoff): verifying-case-math, shipping-prompt-contracts, triaging-reviews-with-evidence, extracting-faithful-corpora, maintaining-project-memory (drafts, see backlog 13a)
/prompts/archive/                           NOT YET IN REPO: v1, v2, v3 prompt files still to add
/cases/food_bank_case_calc1.md  /cases/food_bank_case.tex  /cases/food_bank_case.pdf
/cases/community_garden_case_calc1.md
/cases/alder_creek_bridge_calc1.md
/cases/Taco_Truck_case_study.docx.pdf       Peter's benchmark case
/cases/tests/test_stats_recycling_case.{tex,pdf}
/cases/tests/test_finance_cookie_case.{tex,pdf}
/cases/tests/test_phase2_simplex_case.{tex,pdf}        phase 2 demo (Copper Kettle)
/cases/tests/test_phase2_simplex_flashcards.{tex,pdf}  phase 2 demo (5-card deck)
/tools/tectonic.exe                         local TeX engine (gitignored; XeTeX-based, plan-approved pdflatex alternative)
/cases/archive/                             directory EMPTY: the 4 harder-tier files (sec 6) still to add
/phase1example/problem.txt                  example INPUT to phase 1 (simplex pivot, 4 parts)
/phase1example/{primary.md, supporting_01.md, supporting_02.md, lo_mapping.json, verified_answer.txt}
                                            example 5-file OUTPUT of phase 1 = INPUT contract for phase 2
/plan/pipeline_build_plan.md
/recon/PIPELINE_RECON.md  /recon/REFERENCE_EXTRACTS.md
/recon/pipeline_recon_prompt_claude_code.md  /recon/extraction_prompt_run2.md
```

Compile check for any .tex: `pdflatex -interaction=nonstopmode <file>.tex` must exit 0 on the first pass.
